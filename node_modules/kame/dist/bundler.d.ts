import { Config } from "./config";
export interface IBundler {
    bundle({ input, output, globalName, codeSplittingId, }: {
        input: string;
        output: string;
        globalName?: string;
        codeSplittingId?: string;
    }): {
        warnings: string[];
        writtenFiles: string[];
    };
}
export default function makeBundler(config: Config): {
    new (): IBundler;
};
