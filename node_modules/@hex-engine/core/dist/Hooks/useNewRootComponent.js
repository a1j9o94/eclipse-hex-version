import instantiate from "../instantiate";
import useRootEntity from "./useRootEntity";
export default function useNewRootComponent(componentFunction) {
  var rootEntity = useRootEntity();
  var child = instantiate(componentFunction, rootEntity);
  rootEntity.components.add(child);
  return child;
}