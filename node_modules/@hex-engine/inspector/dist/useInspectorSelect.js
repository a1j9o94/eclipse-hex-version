import { useRootEntity } from "@hex-engine/core";
import Inspector from ".";
export default function useInspectorSelect() {
  var inspector = useRootEntity().getComponent(Inspector);

  if (inspector === undefined) {
    throw new Error("Could not find an inspector component on the root entity");
  }

  var _ref = inspector,
      getSelectMode = _ref.getSelectMode,
      toggleSelectMode = _ref.toggleSelectMode,
      selectEntity = _ref.selectEntity;
  return {
    getSelectMode: getSelectMode,
    inspectEntity: function inspectEntity(entity) {
      selectEntity(entity);
      toggleSelectMode();
    }
  };
}