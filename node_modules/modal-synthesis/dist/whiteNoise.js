"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Create a buffer of generated white noise, of the specified length.
 * @param length
 * @param audioContext
 */
function makeWhiteNoiseBuffer(length, audioContext) {
    var buffer = audioContext.createBuffer(1, length, audioContext.sampleRate);
    var output = buffer.getChannelData(0);
    for (var i = 0; i < length; i++) {
        output[i] = Math.random() * 2 - 1;
    }
    return buffer;
}
exports.makeWhiteNoiseBuffer = makeWhiteNoiseBuffer;
/**
 * Creates an AudioNode that plays the input AudioBuffer.
 * @param buffer The input AudioBuffer
 * @param audioContext the AudioContext used to create the AudioBufferSourceNode
 */
function makeNodeFromBuffer(buffer, audioContext) {
    var sourceNode = audioContext.createBufferSource();
    sourceNode.buffer = buffer;
    return sourceNode;
}
exports.makeNodeFromBuffer = makeNodeFromBuffer;
